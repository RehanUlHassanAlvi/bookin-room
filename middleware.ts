export { default } from "next-auth/middleware";

export const config = {
  matcher: [
    // "/admin/:companyName/:userId",
    // "/admin/:companyName/all-bookinger",
    // "/:companyName/:roomName",
    // "/mine-bookinger",
    // "/admin/[...rest]
  ],
};
